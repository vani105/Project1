rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads and writes to debug the connection
    match /{document=**} {
      allow read, write: if true;
    }
  }
}